# SmartEventTicketing

//.\\venv\\Scripts\\activate.bat


Đúng tầm sinh viên (Student-Level Code): Code đơn giản, mạch lạc, dùng các hàm/cấu trúc cơ bản và phổ biến nhất của [Tên Tech Stack, VD: Django REST Framework / Vue 3 Composition API,SweetAlert2]. Tránh "over-engineering", không dùng các Design Pattern quá phức tạp (như CQRS, Repository Pattern, Abstraction Layer nhiều tầng...) khiến code trở nên khó hiểu khi giáo viên vấn đáp.

Giữ đúng cấu trúc đồ án: Viết chuẩn theo cấu trúc thư mục chuẩn của framework. Giữ nguyên tên field, model, route, store và API đã có trong project, không tự ý đổi tên hay biến tấu cực đoan làm chệch đề tài.

Dễ đọc & Dễ giải thích: Viết code tường minh (clean & straightforward). Bổ sung các dòng comment ngắn gọn giải thích luồng xử lý ở những đoạn quan trọng để tôi dễ nắm bắt và tự tin trả lời khi giáo viên hỏi "Đoạn này xử lý logic gì?".

Trọn vẹn & Đầy đủ: Đưa ra toàn bộ code hoàn chỉnh của các file cần chỉnh sửa hoặc tạo mới, không viết tắt, không để lại comment dạng // TODO: tự làm tiếp.